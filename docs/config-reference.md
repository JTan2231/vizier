# Config Reference (Alias)

Canonical location: `docs/user/config-reference.md`.

This file is a compatibility alias for root-level references used by orientation docs. Update the canonical file in `docs/user/`.
