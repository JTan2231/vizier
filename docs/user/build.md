# Build and Validation

Use the retained commands:

- `vizier init` / `vizier init --check`
- `vizier list`
- `vizier jobs ...`
- `vizier release`

For command reference pages, see `man vizier`, `man vizier-jobs`, `man 7 vizier-workflow`, and `man 7 vizier-workflow-template`.
